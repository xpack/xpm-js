{
  "name": "@<scope>/{{ projectName }}",
  "version": "0.1.0",
  "description": "A source xPack with <your-description-here>",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/<user-id>/{{ gitProjectName }}.git"
  },
  "bugs": {
    "url": "https://github.com/<user-id>/{{ gitProjectName }}/issues/"
  },
  "homepage": "https://github.com/<user-id>/{{ gitProjectName }}/",
  "keywords": [
    "xpack"
  ],
  "author": {
    "name": "{{ author.name }}",
    "email": "{{ author.email }}",
    "url": "{{ author.url }}"
  },
  "license": "MIT",
  "config": {},
  "dependencies": {},
  "devDependencies": {},
  "xpack": {
    "minimumXpmRequired": "{{ xpm.version }}"
  }
}
