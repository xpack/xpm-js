{/* DO NOT EDIT! */}
{/* Automatically generated from xbb-helper/templates/docusaurus/common. */}

{/* ------------------------------------------------------------------------ */}

Similarly to CMake, **{props.name}**, policies are employed to preserve backward-compatible behaviour across multiple releases.

Newer **{props.name}** releases can adjust some functionality to match that of an older release, based on the value of the <code>minimum{String(props.name).charAt(0).toUpperCase() + String(props.name).slice(1)}Required</code> property in <code>{props.fileName}.json</code>.

When such policies are applied, warnings are issued to inform developers about deprecated features, providing them with time to update their packages.

The following policies are available (in reverse chronological order):
